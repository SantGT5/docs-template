/**
 * Global site metadata. Edit this when cloning the template for a new project.
 */
export const siteConfig = {
  /** Short name shown in the header/logo and footer. */
  name: "Docs",
  /** Full title used in the browser tab and the home hero. */
  title: "Docs Template",
  /** One-line description used on the home page and meta tags. */
  description:
    "A clean, component-driven documentation template built with React, Vite and Chakra UI.",
  /** Tagline shown under the hero heading on the home page. */
  tagline: "Write docs in MDX, reuse real components, ship in minutes.",
  /** Link to the project's source repository. */
  github: "https://github.com/your-org/your-repo",
} as const;
